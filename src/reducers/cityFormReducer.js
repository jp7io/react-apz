const initialState = {
  name: '',
  state: '',
  country: ''
};

const cityFormReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
}

export default cityFormReducer;
